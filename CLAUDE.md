# Huginn

Single-file app: everything is in `huginn.html`. No build, no dependencies,
no external scripts. Test by serving the folder over http and walking the
wizard, call mode, export and import by hand. Bump `APP.version` on
behaviour change. Never commit data files; `.gitignore` covers them.
